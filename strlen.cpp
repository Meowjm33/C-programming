#include<stdio.h>
#include<string.h>
int main()
{
	char a[]="hello world";
	int len=strlen(a);
	printf("length of the string is %d",len);///strlen() le length of the text print garcha !!!!
	return 0;
}

